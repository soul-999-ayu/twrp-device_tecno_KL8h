on fs
    # Mount the vendor partition so firmware is available
    mount /vendor

on post-fs
    # After vendor is mounted, load the patched touchscreen module
    # This assumes you are copying the patched .ko to /lib/modules
    insmod /lib/modules/adaptive-ts.ko
